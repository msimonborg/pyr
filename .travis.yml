language: ruby
rvm:
  - 2.3.0
  - 2.3.4
  - 2.4.1
  - ruby-head

script:
  - bundle exec rake

before_install:
  - rm Gemfile.lock
  - gem update --system
  - gem update bundler

notifications:
  email:
    on_success: never
    on_failure: always
